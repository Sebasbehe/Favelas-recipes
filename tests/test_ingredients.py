def test_ingredient_name():
    ingredient = "Tomate"

    assert ingredient != ""


def test_quantity():
    quantity = "2"

    assert quantity != ""


def test_user_id():
    user_id = 1

    assert user_id > 0